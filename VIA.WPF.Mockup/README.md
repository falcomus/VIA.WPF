# VIA.WPF.Mockup

Public aggregate package for reusable mockup applications.

A host such as `flink.Designer` references only this project. It receives both the normal VIA.WPF UI stack and the mockup designer projects transitively.
