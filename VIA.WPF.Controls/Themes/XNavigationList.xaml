<!--=====================================================================================================================
  <copyright file="XNavigationList.xaml" company="VIA.WPF">
    Copyright (c) VIA.WPF. All rights reserved.
  </copyright>
=====================================================================================================================-->

<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:VIA.WPF.Controls"
    xmlns:themes="clr-namespace:VIA.WPF.Themes;assembly=VIA.WPF.Themes">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="/VIA.WPF.Controls;component/Themes/Converters.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style TargetType="{x:Type controls:XNavigationList}">
        <Setter Property="Background" Value="{DynamicResource {x:Static themes:XBrushKeys.NavigationPanelBackground}}" />
        <Setter Property="Foreground" Value="{DynamicResource {x:Static themes:XBrushKeys.NavigationPanelForeground}}" />
        <Setter Property="BorderBrush" Value="{DynamicResource {x:Static themes:XBrushKeys.NavigationPanelBorder}}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="Padding" Value="4" />
        <Setter Property="CornerRadius" Value="8" />
        <Setter Property="HeaderSpacing" Value="8,6,8,10" />
        <Setter Property="FooterSpacing" Value="8,10,8,6" />
        <Setter Property="ItemPadding" Value="10,7" />
        <Setter Property="ItemMargin" Value="0,1" />
        <Setter Property="ItemCornerRadius" Value="4" />
        <Setter Property="SelectedItemBackground" Value="{DynamicResource {x:Static themes:XBrushKeys.NavigationPanelSelectedItemBackground}}" />
        <Setter Property="SelectedItemForeground" Value="{DynamicResource {x:Static themes:XBrushKeys.NavigationPanelSelectedItemForeground}}" />
        <Setter Property="ItemHoverBackground" Value="{DynamicResource {x:Static themes:XBrushKeys.NavigationPanelItemHoverBackground}}" />
        <Setter Property="ShowSelectionIndicator" Value="True" />
        <Setter Property="SelectionIndicatorBrush" Value="{DynamicResource {x:Static themes:XBrushKeys.Accent}}" />
        <Setter Property="SelectionIndicatorWidth" Value="3" />
        <Setter Property="SelectionIndicatorCornerRadius" Value="0,2,2,0" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
        <Setter Property="ScrollViewer.CanContentScroll" Value="True" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="UseLayoutRounding" Value="True" />

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type controls:XNavigationList}">
                    <Border
                        Padding="{TemplateBinding Padding}"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="{TemplateBinding CornerRadius}"
                        SnapsToDevicePixels="True">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <ContentPresenter
                                Grid.Row="0"
                                Margin="{TemplateBinding HeaderSpacing}"
                                HorizontalAlignment="Stretch"
                                Content="{TemplateBinding Header}"
                                ContentTemplate="{TemplateBinding HeaderTemplate}"
                                Visibility="{Binding Header, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource NullToVisibilityConverter}}" />

                            <Border
                                Grid.Row="1"
                                Height="1"
                                Margin="8,0,8,6"
                                Background="{TemplateBinding BorderBrush}"
                                IsHitTestVisible="False"
                                Opacity="0.65"
                                Visibility="{Binding Header, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource NullToVisibilityConverter}}" />

                            <ScrollViewer
                                Grid.Row="2"
                                CanContentScroll="{TemplateBinding ScrollViewer.CanContentScroll}"
                                Focusable="False"
                                HorizontalScrollBarVisibility="{TemplateBinding ScrollViewer.HorizontalScrollBarVisibility}"
                                VerticalScrollBarVisibility="{TemplateBinding ScrollViewer.VerticalScrollBarVisibility}">
                                <ItemsPresenter />
                            </ScrollViewer>

                            <Border
                                Grid.Row="3"
                                Height="1"
                                Margin="8,6,8,0"
                                Background="{TemplateBinding BorderBrush}"
                                IsHitTestVisible="False"
                                Opacity="0.65"
                                Visibility="{Binding Footer, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource NullToVisibilityConverter}}" />

                            <ContentPresenter
                                Grid.Row="4"
                                Margin="{TemplateBinding FooterSpacing}"
                                HorizontalAlignment="Stretch"
                                Content="{TemplateBinding Footer}"
                                ContentTemplate="{TemplateBinding FooterTemplate}"
                                Visibility="{Binding Footer, RelativeSource={RelativeSource TemplatedParent}, Converter={StaticResource NullToVisibilityConverter}}" />
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>

        <Style.Triggers>
            <Trigger Property="Variant" Value="{x:Static controls:XNavigationListVariant.Surface}">
                <Setter Property="Background" Value="{DynamicResource {x:Static themes:XBrushKeys.SurfaceRaised}}" />
                <Setter Property="Foreground" Value="{DynamicResource {x:Static themes:XBrushKeys.TextPrimary}}" />
                <Setter Property="BorderBrush" Value="{DynamicResource {x:Static themes:XBrushKeys.BorderSubtle}}" />
                <Setter Property="SelectedItemBackground" Value="{DynamicResource {x:Static themes:XBrushKeys.StateSelected}}" />
                <Setter Property="SelectedItemForeground" Value="{DynamicResource {x:Static themes:XBrushKeys.SelectionForeground}}" />
                <Setter Property="ItemHoverBackground" Value="{DynamicResource {x:Static themes:XBrushKeys.StateHover}}" />
            </Trigger>

            <Trigger Property="Variant" Value="{x:Static controls:XNavigationListVariant.Dark}">
                <Setter Property="Background" Value="{DynamicResource {x:Static themes:XBrushKeys.NavigationPanelBackground}}" />
                <Setter Property="Foreground" Value="{DynamicResource {x:Static themes:XBrushKeys.NavigationPanelForeground}}" />
                <Setter Property="BorderBrush" Value="{DynamicResource {x:Static themes:XBrushKeys.NavigationPanelBorder}}" />
                <Setter Property="SelectedItemBackground" Value="{DynamicResource {x:Static themes:XBrushKeys.NavigationPanelSelectedItemBackground}}" />
                <Setter Property="SelectedItemForeground" Value="{DynamicResource {x:Static themes:XBrushKeys.NavigationPanelSelectedItemForeground}}" />
                <Setter Property="ItemHoverBackground" Value="{DynamicResource {x:Static themes:XBrushKeys.NavigationPanelItemHoverBackground}}" />
            </Trigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>
